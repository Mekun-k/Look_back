

    <div class="container-fluid py-4 mw-md">
      <header>
        <nav class="navbar navbar-expand navbar-light">
          <div id="Navber">
          <ul class="navbar-nav">
            <% if user_signed_in? %>
              <li class="nav-item active">
                <strong><%= link_to current_user.name, pages_show_path, class: 'nav-link' %></strong>
              </li>
              <li class="nav-item active">
                <%= link_to 'ログアウト', destroy_user_session_path, class: 'nav-link', method: :delete %>
              </li>
            <% else %>
              <li class="nav-item active">
                <%= link_to 'ログイン', new_user_session_path, class: 'nav-link' %>
              </li>
            <% end %>
          </ul>
          </div>
        </nav>
      </header>
    </div>