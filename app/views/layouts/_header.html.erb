
      <header>
        <nav class="navbar navbar-expand navbar-light">
          <div id="Navber">
          <ul class="navbar-nav">
            <% if user_signed_in? %>
              <li class="nav-item active">
                <%= link_to (t 'defaults.account'), edit_user_path(current_user), class: 'nav-link' %>
              </li>
              <li class="nav-item active">
                <%= link_to (t 'layouts._header.top'), root_path, class: 'nav-link' %>
              </li>
              <li class="nav-item active">
                <%= link_to (t 'defaults.logout'), destroy_user_session_path, class: 'nav-link', method: :delete %>
              </li>

            <% else %>
              <li class="nav-item active">
                <%= link_to (t 'defaults.login'), new_user_session_path, class: 'nav-link' %>
              </li>
              <li class="nav-item active">
                <%= link_to 'ゲストログイン（閲覧用）', users_guest_sign_in_path, method: :post, class: 'nav-link' %>
              </li>
            <% end %>
          </ul>
          </div>
        </nav>
      </header>
